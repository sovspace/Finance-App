<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout

    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="20dp"
    android:background="@drawable/rounded_frame_stock"
    android:padding="15dp">


    <TextView
        android:id="@+id/stockName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:textColor="@color/primaryTextColor"
        android:textSize="25sp"
        android:textStyle="bold"
        tools:text="Stock name" />

    <TextView
        android:id="@+id/stockAmount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/stockName"
        android:layout_marginStart="10dp"
        android:paddingTop="2dp"
        android:paddingBottom="5dp"
        android:textColor="@color/primaryTextColor"
        android:textSize="18sp"
        tools:text="Stock amount" />

    <TextView
        android:id="@+id/stockPurchasePrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/stockAmount"
        android:layout_marginStart="10dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:textColor="@color/primaryTextColor"
        android:textSize="18sp"
        tools:text="Stock purchase price" />

    <TextView
        android:id="@+id/stockCurrentPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/stockPurchasePrice"
        android:layout_alignParentStart="true"
        android:layout_marginStart="10dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:textColor="@color/primaryTextColor"
        android:textSize="18sp"
        tools:text="Stock current price" />

    <TextView
        android:id="@+id/stockLastUpdatedTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/stockCurrentPrice"
        android:layout_marginStart="10dp"
        android:layout_marginTop="2dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:textColor="@color/primaryTextColor"
        android:textSize="18sp"
        tools:text="Stock name" />

</RelativeLayout>
